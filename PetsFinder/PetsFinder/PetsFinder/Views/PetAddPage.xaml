<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PetsFinder.Views.PetAddPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" VerticalOptions="Center" Padding="20,20">
            <Entry Text="{Binding Name}" Placeholder="Name"/>
            <Entry Text="{Binding Description}" Placeholder="Description"/>
            <Entry Text="{Binding City}" Placeholder="City"/>
            <Entry Text="{Binding Sex}" Placeholder="Sex"/>
            <Entry Text="{Binding Image}" Placeholder="Image"/>
            <Entry Text="{Binding Telephone}" Placeholder="Ex:58009010"/>
            <StackLayout HorizontalOptions="FillAndExpand" Margin="0,20,0,0">
                <Button Text="Add" Clicked="SavePet"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>